# Base URL of the backend API. Defaults to "/api" (proxied by vite dev server
# and nginx in production). Override with the full URL when the API is served
# from a different origin.
VITE_API_URL=/api
VITE_GOOGLE_CLIENT_ID=
